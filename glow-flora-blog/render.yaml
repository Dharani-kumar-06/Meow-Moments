services:
  - type: web
    name: glow-flora-blog
    env: node
    buildCommand: npm run install:frontend && npm run install:backend && npm run build
    startCommand: npm start
    rootDir: .
    envVars:
      - key: NODE_ENV
        value: production
